//defines 'sentence' variable as a string, concatenates more text to it
function myFunction() {
    document.getElementById("demo").innerHTML = "Practice";
}

//displays value of the "sentence" variable at the HTML element w/ ID "Concatenate"
function myFunction() {
    var sentence = "Learning";
    sentence += " a lot";
    document.getElementById("Concatenate").innerHTML = sentence;
}